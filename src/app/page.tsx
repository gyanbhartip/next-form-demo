import DealForm from "_/components/DealForm";

const HomePage = () => {
  return (
    <main className="flex min-h-screen bg-gradient-to-b from-[#2e026d] to-[#995f8f]">
      <DealForm />
    </main>
  );
};

export default HomePage;
